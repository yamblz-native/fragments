<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.yamblz.netizen.provider">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".contentprovider.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:authorities="ru.yamblz.netizen.provider.contentprovider.BardContentProvider"
            android:name=".contentprovider.BardContentProvider"
            android:exported="true"/>

    </application>


</manifest>
